# EchoLine Recheck Report

Date: 2026-07-02  
Scope: post-audit verification after `deep-review` and `strict-audit`

## Summary

No new P0/P1/P2 issues found in this pass.

The project still matches the current closure state: mainline tasks are closed, local unit/build gates pass, and the remaining gaps are environment-limited or P3/P4 product polish.

## Areas Rechecked

- Current state docs: `CURRENT_STATE.md`, `NEXT_ACTIONS.md`, `DONE.md`, `BLOCKERS.md`, `TASKS.md`, recent `PROGRESS_LOG.md`.
- Prior audit artifacts: `reports/deep-review-final.md`, `reports/strict-audit/final.md`, round 02 findings.
- Backend paths: server route wiring, auth/rate limit middleware, GraphQL prototype, reactions, threads, forward, media download, ads impression cap, payment/entitlement, ACK/WS, outbox publisher.
- Frontend paths: auth refresh, WS reconnect/ACK handling, optimistic send, upload checksum/pending state, channel publish gating, mocked Playwright flows.

## Verification

| Command | Result |
|---|---|
| `cd backend && go test ./...` | PASS |
| `cd backend && go vet ./...` | PASS |
| `cd backend && go test -race ./internal/rate_limit ./internal/realtime ./internal/message ./internal/outbox ./internal/graph ./internal/payment` | PASS |
| `cd frontend && npm run lint` | PASS |
| `cd frontend && npm run build` | PASS |
| `cd frontend && npm audit --omit=dev` | PASS, 0 vulnerabilities |
| `cd frontend && npm run test:e2e` | PASS, 4 tests |

Note: Playwright Chromium was missing locally on the first E2E run. `npx playwright install chromium` fixed the environment, then the test suite passed.

## Revalidated Previous Fixes

- Rate limit is active without Redis through `MemoryLimiter`.
- `X-Forwarded-For` is ignored unless `TRUSTED_PROXY=true`.
- GraphQL `addReaction` resolves the message conversation and checks membership before writing.
- GraphQL `sendMessage` is behind authenticated per-user rate limiting.
- Ads impression cap uses a transaction and `SELECT ... FOR UPDATE`.
- REST reactions, thread replies, forward, media download, ACK, and paid-channel entitlement paths enforce membership/owner/admin checks where expected.
- Frontend WS reconnect uses refreshed token state, sends `message.ack`, and deduplicates optimistic sends by `client_msg_id`.

## Remaining Risks

| Priority | Area | Status |
|---|---|---|
| P3 | Full `make smoke-full` | Still blocked without local Docker/Postgres/API stack |
| P3 | Go integration tests | `backend/tests/integration_test.go` is still placeholder/skip-only; real flow lives in smoke script |
| P3 | Outbox stale `processing` reaper | Still open optional reliability hardening |
| P3 | Attachment download UI | Backend exists, frontend still upload-only |
| P3 | JWT in WS query string | Known browser limitation; future short-lived WS ticket recommended |
| P3 | Redis limiter INCR+Expire atomicity | Production Redis Lua script recommended |
| P3 | Edit/recall membership errors | API semantics polish: map some cases to 403 instead of 400 |
| P4 | Playwright mock noise | E2E passes, but Vite logs proxy `ECONNREFUSED` for a few unmocked startup requests |

## Conclusion

Stop condition remains satisfied: this recheck found no open P0/P1/P2 regressions after the earlier audit fixes. Recommended next work is still local full-stack smoke or one of the documented P3 improvements.
